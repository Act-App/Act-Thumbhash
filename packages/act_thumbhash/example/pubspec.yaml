name: act_thumbhash_example
description: Example for act_thumbhash
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

# The example always runs against the package source in this repository, not
# a published version.
dependencies:
  act_thumbhash:
    path: ../

dev_dependencies:
  lints: ^6.0.0